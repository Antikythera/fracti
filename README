A small python program that renders fractal shapes. It uses the cpu to render image so it is not very smooth. Planning to learn how to make it run on gpu.
Basically, was learning python here.

fracti.py --help
